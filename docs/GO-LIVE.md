# ขึ้นระบบจริง: branch member-polish

เขียน 16 ก.ย. 2569 · สถานะ: **ยังไม่ขึ้นระบบจริง** โค้ดอยู่บน branch `member-polish` เท่านั้น

## ทำไมยังไม่ขึ้นทันที

งานรอบนี้แก้เรื่องเงินของลูกค้า 3 จุด ได้แก่ ตัวตัดแต้มหมดอายุรายคืน การยืนยันแลกของ และการให้/หักแต้ม
ทุกจุดพิสูจน์กับ Postgres ที่รันในเครื่องแล้ว แต่ยังไม่เคยรันกับฐานข้อมูลจริงของร้าน (Neon)
ตัวตัดแต้มรันเองทุกคืนเวลา 01:00 น. ถ้าขึ้นระบบแล้วมีอะไรผิด แต้มลูกค้าจริงจะถูกตัดก่อนมีใครเห็น
จึงต้องมีขั้นตรวจแบบ "คำนวณแต่ไม่เขียน" กับข้อมูลจริงก่อนหนึ่งรอบ

## ก่อนขึ้นระบบ (ทำบนเครื่องนี้)

```
npm test
npx tsc --noEmit
node scripts/check-tiers.mjs
node scripts/check-contrast.mjs
node scripts/validate-line-messages.mjs
npx next build
```
ทุกคำสั่งต้องผ่าน

## ขั้นตอนขึ้นระบบ

1. ขึ้น server จาก branch แล้ว merge เข้า main ให้ Vercel deploy ก่อน เพื่อให้ migration เพิ่ม `backfill_from`/`backfill_done_at` และ API รองรับ backfill
2. หลัง server พร้อมแล้ว ปล่อย `hero_points_backfill.js` และ `hero_points_watch.js` ที่เครื่องร้าน บอทเริ่มทำงานได้ทันที; ถ้ายังเจอ server รุ่นเก่าที่ GET ไม่มี field `backfill` จะไม่ทำอะไร
3. **ภายในวันเดียวกัน ก่อน 01:00 น.** เรียกตัวตัดแต้มแบบไม่เขียน (dry run) กับข้อมูลจริง แล้วดูตัวเลข
   ```bash
   curl -H "Authorization: Bearer $CRON_SECRET" "https://<โดเมนจริง>/api/cron/expire-points?dry=1"
   ```
   - จำนวนสมาชิกที่จะถูกตัด และแต้มรวมที่จะตัด ต้องสมเหตุสมผล (ร้านเพิ่งเริ่มระบบ ควรเป็น 0 หรือใกล้ 0)
   - ถ้าตัวเลขผิดปกติ ให้ปิด cron `expire-points` ใน vercel.json ก่อน แล้วค่อยหาสาเหตุ
4. ทดสอบด้วยบัญชี LINE ของเจ้าของร้านเอง (ใช้โควตา push น้อยที่สุด)
   - เปิดบัตรสมาชิกในแอป LINE เห็นตัวเลขตรงกับหน้าแอดมิน
   - ถ้ายังไม่ผูกรหัส ต้องขึ้นจอ "รอพนักงานยืนยันตัวตน" · ผูกในหน้าแอดมินแล้วเปิดใหม่ จอนี้ต้องหาย
   - แลกของรางวัลชิ้นถูกสุด 1 ครั้ง → หน้าของรางวัลเห็นแต้มที่จองไว้และเลข #REQ → ยืนยันในแอดมิน → แต้มลดครั้งเดียว มีประวัติ 1 รายการ
   - กดยืนยันซ้ำในแอดมิน ต้องขึ้นว่าดำเนินการไปแล้ว แต้มไม่ลดอีก
5. เช็คโควตา LINE หลังทดสอบ (ควรใช้ไปไม่เกิน 5 ข้อความ)

## ถ้าต้องถอยกลับ

Vercel → Deployments → เลือก deployment ก่อนหน้า → Instant Rollback
ฐานข้อมูล: งานรอบนี้ไม่ลบคอลัมน์หรือตาราง เพิ่มเฉพาะดัชนี ตาราง `hero_pending_bills` และคอลัมน์ `users.backfill_from`/`users.backfill_done_at` ถอยโค้ดกลับได้โดยไม่ต้องแก้ฐานข้อมูล

## การตัดสินใจเรื่องกติกา (เจ้าของร้านมอบให้ตัดสินแทน 16 ก.ย. 69)

| เรื่องที่ลูกค้า/ผู้ตรวจยกขึ้นมา | ตัดสิน | เหตุผล |
|---|---|---|
| บิลที่ซื้อหลังสมัครแต่ก่อนพนักงานยืนยันตัวตนไม่ได้แต้ม | **ทำแล้ว: ให้แต้มย้อนหลัง** เฉพาะบิลตั้งแต่วันสมัคร ย้อนหลังไม่เกิน 30 วัน และให้ครั้งเดียวตอนผูกรหัสครั้งแรก | server เก็บช่วงวันที่และกันบิลซ้ำ บอทร้านอ่าน Hero แบบ read-only แล้วส่งผ่านเส้นทางคิดแต้ม/โบนัสเดิม |
| Bronze ไม่มีส่วนลด | คงเดิม | ป้ายและตารางสิทธิ์พิมพ์ไปแล้ว · Bronze ได้คูปองวันเกิด 100 แต้มอยู่แล้ว ให้หน้าเว็บบอกให้ชัดแทน |
| เหล็กไม่มีส่วนลดหน้าร้าน | คงเดิม | ตัดสินไว้แล้ว 14 ก.ย. 69 กันลดซ้ำกับราคาที่ต่อกันหน้าร้าน · ได้แต้มพิเศษแทน |
| ลูกค้าไม่รู้ว่า 1 แต้มเท่ากับกี่บาท | **ยกตัวอย่างจากของรางวัลจริง** (เช่น "100 แต้ม แลกส่วนลดเงินสด 100 บาท") ไม่พิมพ์อัตราตายตัว | แก้ 17 ก.ย. 69: เดิมตั้งใจเขียน "1 แต้ม = 1 บาท" แต่ยังไม่ได้ดูรายการของรางวัลจริงของร้าน ตัวอย่างจากรายการจริงถูกต้องเสมอ |
| ลดระดับชั่วคราวเมื่อหายไปเกินปี ลูกค้างง | คงกติกา | หน้าเว็บเตือนล่วงหน้าแล้ว และ LINE เตือนตอนครบ 11 เดือนอยู่แล้ว |
| ลูกค้ายกเลิกคำขอแลกของเอง | ยังไม่เปิด | ให้พนักงานยกเลิกให้ ลดโอกาสกดเล่น · ทบทวนเมื่อมีสมาชิกจริงใช้งาน |

## งานถัดไปที่ยังไม่ทำ

1. ตัวตัดแต้มแบบ dry run (ต้องมีก่อนขึ้นระบบ)
2. ข้อความบนหน้าเว็บ: 1 แต้ม = 1 บาท และสิทธิ์ของ Bronze
3. เทียบกับหน้าเว็บคู่แข่งแบบปิดชื่อ (ภาพอยู่ใน docs/benchmarks)
